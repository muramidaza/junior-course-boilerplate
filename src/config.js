const endpoint = 'https://course-api.school.csssr.com';

export const API = {
   products: `${endpoint}/products`,
   save: `${endpoint}/save`
}

export const DEFAULT_DISCOUNT = 0;
export const GOODS_IN_PAGE = 6;
export const MAX_RATING = 5;
export const SUB_PRICE_CONTENT = 'руб.';